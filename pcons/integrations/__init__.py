# SPDX-License-Identifier: MIT
"""Integrations with external systems.

Currently:

- :mod:`pcons.integrations.rez` — read a rez resolve from inside pcons,
  and provide a rez ``build_system`` plugin so ``rez-build`` can drive
  pcons.
"""
