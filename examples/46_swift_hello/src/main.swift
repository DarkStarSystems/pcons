// Top-level code: this file is the program entry point.
// greeter.swift's functions are visible without imports because all
// files in a target compile as one Swift module.
print(greet(name: "pcons"))
