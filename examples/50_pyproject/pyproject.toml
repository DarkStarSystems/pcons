[build-system]
requires = ["pcons", "conan"]
build-backend = "pcons.pyproject"

[project]
name = "pcons_hello_ext"
version = "0.0.1"
requires-python = ">=3.11"

[tool.pcons]
variant = "release"
# variables = { TOOLCHAIN = "gcc" }
