// SPDX-License-Identifier: MIT
// Partition INTERFACE unit (in a .cpp file).
// pcons identifies this as a module-providing TU from the scanner output,
// not from the file extension, and emits the correct compile flags.
export module Calc:Constants;

export constexpr int kAnswer = 42;
